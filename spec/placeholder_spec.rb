# This spec is in here just to make sure that the RSpec works and should be deleted
# when we have real specs against real code.

describe Fixnum, "add" do
  it 'adds 1 + 1' do
    (1 + 1).should == 2
  end
end
